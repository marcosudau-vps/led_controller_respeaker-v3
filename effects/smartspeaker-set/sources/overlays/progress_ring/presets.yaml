presets:
  progress_ring_green:
    title: Progress — Green
    params: {color: "#00C066", brightness: 0.90}
  progress_ring_blue:
    title: Progress — Blue
    params: {color: "#3399FF", brightness: 0.85}
